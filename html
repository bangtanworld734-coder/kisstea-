<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width,initial-scale=1" />
<title>Happiest B'day Kisstea ✨</title>
<link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;700&family=Quicksand:wght@300;400;600&display=swap" rel="stylesheet">
<style>
  body{
    margin:0;
    font-family:"Quicksand", sans-serif;
    background:linear-gradient(180deg,#050509,#0b0f16,#121826);
    color:#f2eaff;
    overflow-x:hidden;
    text-align:center;
  }

  /* Smooth fade-in for all sections */
  section, header, .hero, .letter, .gallery, .game-box {
    opacity:0;
    transform:translateY(20px);
    animation: fadeInUp 1.3s ease forwards;
  }
  @keyframes fadeInUp {
    to {opacity:1; transform:translateY(0);} 
  }

  /* Loading Screen */
  #loader{
    position:fixed;top:0;left:0;width:100%;height:100%;
    background:#050509;
    display:flex;align-items:center;justify-content:center;
    flex-direction:column;
    z-index:9999;
    color:#d9c9ff;
    font-size:1.2rem;
  }
  .loader-moon{
    width:80px;height:80px;
    border-radius:50%;
    background:radial-gradient(circle,#ffffff,#d9c9ff,#8a6bff);
    animation:pulse 2s infinite ease-in-out;
    box-shadow:0 0 40px #9e7cff;
  }
  @keyframes pulse{0%{transform:scale(1);}50%{transform:scale(1.15);}100%{transform:scale(1);}}

  header{margin:30px 0;animation-delay:0.3s;}
  .title{font-family:"Playfair Display",serif;font-size:3rem;color:#e6d4ff;}
  .subtitle{color:#bba3ff;}

  /* Floating moon */
  .floating-moon{
    position:fixed;
    top:40px; right:40px;
    width:120px;height:120px;
    background:radial-gradient(circle,#f7f3ff,#cdbaff,#9d83ff);
    border-radius:50%;
    box-shadow:0 0 30px #a68bff;
    animation:floatMoon 6s ease-in-out infinite;
    z-index:2;
  }
  @keyframes floatMoon{0%{transform:translateY(0);}50%{transform:translateY(-20px);}100%{transform:translateY(0);}}

  /* Stars */
  .star{
    position:fixed;width:3px;height:3px;border-radius:50%;
    background:white;opacity:0.8;
    animation:twinkle 3s infinite;
  }
  @keyframes twinkle{0%{opacity:0.2;}50%{opacity:1;}100%{opacity:0.3;}}

  .hero{
    width:100%;max-width:900px;margin:0 auto;height:320px;border-radius:18px;
    overflow:hidden;box-shadow:0 0 40px rgba(150,120,255,0.3);
    animation-delay:0.4s;
  }
  .hero img{width:100%;height:100%;object-fit:cover;filter:brightness(0.7);} 

  /* Buttons */
  .btn{background:#1d1b2e;color:#e8dbff;padding:12px 24px;border-radius:999px;
    border:1px solid #6a4fff;cursor:pointer;font-weight:600;margin:10px;
    box-shadow:0 0 14px rgba(120,90,255,0.4);} 
  .btn:hover{background:#2d2850;}

  /* Letter */
  .letter{max-width:720px;margin:30px auto;background:#1a1930;padding:24px;
    border-radius:14px;box-shadow:0 0 30px rgba(140,110,255,0.3);
    display:none;color:#e9ddff;animation-delay:0.5s;}

  /* Cake Game */
  .game-box{max-width:720px;margin:40px auto;text-align:center;padding:20px;
    background:#1a1930;border-radius:14px;box-shadow:0 0 30px rgba(140,110,255,0.2);
    animation-delay:0.7s;} 

  /* Gallery */
  .gallery{
    max-width:900px;margin:50px auto;
    display:grid;grid-template-columns:repeat(auto-fit,minmax(180px,1fr));
    gap:18px;padding:20px;animation-delay:0.6s;
  }
  .gallery img{
    width:100%;border-radius:12px;box-shadow:0 0 20px rgba(160,130,255,0.3);
    transition:0.4s;
  }
  .gallery img:hover{transform:scale(1.05);}

  /* Petals */
  .petal-container{position:fixed;top:0;left:0;width:100%;height:100%;pointer-events:none;overflow:hidden;}
  .petal{position:absolute;top:-10px;width:15px;height:15px;background:radial-gradient(circle,#d9a4ff,#9b5bff);
    border-radius:70% 30% 70% 30%;opacity:0.9;animation:fall linear forwards;} 
  @keyframes fall{0%{transform:translateY(0) rotate(0deg);}100%{transform:translateY(110vh) rotate(360deg);} }

  /* Mobile improvements */
  @media(max-width:600px){
    .floating-moon{top:20px;right:20px;width:90px;height:90px;}
    .title{font-size:2.4rem;}
    .hero{height:240px;}
    .btn{padding:10px 20px;font-size:0.9rem;}
  }

</style>
</head>
<body>
<div id="loader"><div class="loader-moon"></div><p>loading your night meadow...</p></div>
<div class="floating-moon"></div>
<header>
  <div class="title">happy b'day, kisstea ✨</div>
  <div class="subtitle">a dreamy night meadow just for you</div>
</header>

<div class="hero"><img src="images/hero.jpg"></div>

<section style="margin-top:20px;">
  <button class="btn" id="openLetter">Open your birthday note 💌</button>
</section>

<div class="letter" id="letter">
  <p>happy birthday kisstea! being roommates for those few months is gonna shine bright in my memories. here's to more adventures w you. i miss you and shillong trip toh karke hi rahenge.</p>
</div>

<h2 style="margin-top:50px;">📸 Memory Polaroids</h2>
<div class="gallery">
  <div class="polaroid"><img src="images/pic1.jpg"><div class="caption">memory 1</div></div>
  <div class="polaroid"><img src="images/pic2.jpg"><div class="caption">memory 2</div></div>
  <div class="polaroid"><img src="images/pic3.jpg"><div

<div class="game-box">
  <h2>🎂 Birthday Cake</h2>
  <div id="cake">🍰</div>
  <p>Tap the cake 5 times for a tiny surprise!</p>
  <button class="btn" id="cakeBtn">Tap 🍰</button>
  <p id="cakeMsg"></p>
</div>

<audio id="bg-music" src="salvatore.mp3" autoplay loop></audio>

<script>
// Loader Fade-Out
window.onload=function(){document.getElementById('loader').style.display='none';};

// Stars
for(let i=0;i<80;i++){
  let star=document.createElement('div');
  star.className='star';
  star.style.left=Math.random()*100+'vw';
  star.style.top=Math.random()*100+'vh';
  star.style.animationDelay=Math.random()*3+'s';
  document.body.appendChild(star);
}

// Letter Toggle
const letter=document.getElementById('letter');
const btn=document.getElementById('openLetter');
btn.onclick=()=>{letter.style.display=letter.style.display==='none'?'block':'none';};

// Cake Game
let taps=0;
const cakeMsg=document.getElementById('cakeMsg');
document.getElementById('cakeBtn').onclick=()=>{
  taps++;
  if(taps>=5){cakeMsg.textContent="✨ kisstea unlocked cosmic happiness!"; }
};

// Petals
const petalContainer=document.querySelector('.petal-container');
function createPetal(){
  const petal=document.createElement('div');
  petal.classList.add('petal');
  petal.style.left=Math.random()*100+'vw';
  petal.style.animationDuration=4+Math.random()*5+'s';
  petal.style.opacity=Math.random();
  petalContainer.appendChild(petal);
  setTimeout(()=>petal.remove(),9000);
}
setInterval(createPetal,400);
</script>

<div class="petal-container"></div>
</body>
</html>
